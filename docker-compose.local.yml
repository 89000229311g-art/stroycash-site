services:
  nginx:
    image: nginx:alpine
    ports:
      - "8089:80"
    volumes:
      - ./stroycash:/var/www/html:ro
      - ./nginx.local.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php

  php:
    image: php:8.2-fpm-alpine
    volumes:
      - ./stroycash:/var/www/html
